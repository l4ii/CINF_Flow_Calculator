{
  "name": "flow-calculation-tool",
  "version": "1.0.0",
  "description": "长沙院浆体管道临界流速计算工具",
  "main": "electron/main.js",
  "scripts": {
    "setup": "npm install && npm install --prefix frontend",
    "start": "electron .",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:python": "node scripts/build-python.js",
    "clean": "node scripts/clean.js",
    "dist": "npm run build --prefix frontend && node frontend/node_modules/electron-builder/cli.js",
    "dist:win": "npm install && python scripts/convert-license-to-gbk.py && npm run build --prefix frontend && node scripts/clean.js release && node frontend/node_modules/electron-builder/cli.js --win",
    "dist:win:full": "npm run build:python && npm run dist:win",
    "dist:mac": "npm run build --prefix frontend && node scripts/clean.js release && node frontend/node_modules/electron-builder/cli.js --mac",
    "dist:linux": "npm run build --prefix frontend && node scripts/clean.js release && node frontend/node_modules/electron-builder/cli.js --linux",
    "disk:win": "npm run dist:win",
    "disk:mac": "npm run dist:mac",
    "disk:linux": "npm run dist:linux",
    "pack": "npm run build --prefix frontend && node frontend/node_modules/electron-builder/cli.js --dir",
    "start:backend": "python backend/app.py"
  },
  "keywords": ["electron", "react", "calculation"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron-updater": "^6.7.3"
  },
  "devDependencies": {
    "electron": "28.0.0"
  }
}
